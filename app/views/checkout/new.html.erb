<h2>Welcome to checkout</h2>
<% @cart.shopping_cart_items.each do |cart| %>
  <div class="images-r">
    <%= image_tag cart.item.file.url, style: 'width: 60px; height: auto' %>
    </div>
  <%= cart.item.name %>
<% end %>
